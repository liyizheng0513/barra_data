# -*- coding:utf-8 -*-

import statsmodels.api as sm
import os
from ConstValue import (BARRA_PATH,
                        FACTOR_LIST
                        )

def neutralize(factor, tradeDate, field=)